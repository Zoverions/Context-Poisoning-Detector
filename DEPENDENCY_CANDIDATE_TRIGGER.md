# Dependency candidate verification trigger

Temporary review-only file used to exercise the read-only dependency candidate workflow against the current hardening branch. It carries no runtime or dependency change and should not be merged into the hardening branch.

Iteration 2 verifies the compact candidate while preserving registry `resolved` URLs and `integrity` hashes.
